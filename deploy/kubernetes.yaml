apiVersion: apps/v1
kind: Deployment
metadata:
  name: reach-testbed-github-marketplace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reach-testbed-github-marketplace
  template:
    metadata:
      labels:
        app: reach-testbed-github-marketplace
    spec:
      containers:
        - name: app
          image: example.invalid/reach-testbed-github-marketplace:synthetic
          securityContext:
            privileged: true
            allowPrivilegeEscalation: true
            readOnlyRootFilesystem: false
          env:
            - name: REACH_TESTBED_SERVICE_TOKEN
              value: rtg_k8s_synthetic_token_not_real
